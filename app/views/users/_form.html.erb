<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :addresses, "住所" %><br>
    <%= f.fields_for :addresses do |builer| %>
      <%= render "address_fields", f: builer %>
    <% end %>
    <%= link_to_add_fields "住所追加", f, :addresses  %>
  </div>
  <div class="field">
    <%= f.label :tasks, "タスク" %><br>
    <%= f.fields_for :tasks do |ff| %>
      <%= ff.select :select_title, Task::TITLES, {include_blank: false} %>
    <% end %>
    <%= hide_add_fields f, :tasks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
